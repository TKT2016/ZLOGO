using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;
using System.Windows.Forms;
//using 第一个爬行;

namespace ZLogoEngine
{
    static class Program
    {
        /// <summary>
        /// 应用程序的主入口点。
        /// </summary>
        [STAThread]
        static void Main()
        {
            Console.WriteLine("ZLogoEngine run end ...");
           /* Application.EnableVisualStyles();
            Application.SetCompatibleTextRenderingDefault(false);
            //Application.RunZLogo(new TurtleTestForm());
           // TurtleFormT1 turtleFormT1 = new TurtleFormT1();
          //  Application.RunZLogo(turtleFormT1);

            第一个爬行 T2 = new 第一个爬行();
            Application.Run(T2);

            //turtleFormT1.RunZLogo();*/
        }
    }
}
